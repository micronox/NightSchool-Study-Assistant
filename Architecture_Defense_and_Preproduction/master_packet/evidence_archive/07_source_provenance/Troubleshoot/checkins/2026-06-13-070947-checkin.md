# Check-In 2026-06-13 07:09:47

- Automation: `wsl-stack-check-in`
- Scope: verify whether revived local APIs remain reachable and whether any
  containerized inference surfaces returned

## Executive Delta

The live state remains consistent:

- Commander local API on `8645` is still up
- Apollo local API on `8646` is still up
- both still require authentication
- Ollama is still up on `11434`
- Docker/containerized inference surfaces are still down

The strongest new evidence this round is that both channel-directory files
advanced again this morning, while both gateway-state files remain frozen on
June 12.

## API Reachability

### Commander

Request to:

- `http://127.0.0.1:8645/v1/models`

Result:

- `401 Unauthorized`

### Apollo

Request to:

- `http://127.0.0.1:8646/v1/models`

Result:

- `401 Unauthorized`

### Interpretation

This reconfirms the local APIs are still alive and enforcing auth.

## Port / Service Snapshot

Listening now:

- `8645`
  - Commander
  - PID `30756`
- `8646`
  - Apollo
  - PID `30732`
- `11434`
  - Ollama
  - PID `66340`

Still not listening:

- `3000`
- `3001`
- `8092`
- `9090`

`docker ps` again returned no running containers.

## File-Timestamp Drift

### Freshly moving

- Commander `channel_directory.json`
  - `2026-06-13 7:04:56 AM`
- Apollo `channel_directory.json`
  - `2026-06-13 7:05:34 AM`

### Still frozen

- Commander `gateway_state.json`
  - `2026-06-12 11:11:31 AM`
- Apollo `gateway_state.json`
  - `2026-06-12 11:11:10 AM`

### Interpretation

This continues the now-repeated pattern:

- live API processes continue to exist
- routing metadata continues to refresh
- gateway state bookkeeping remains frozen

That makes the routing surfaces more trustworthy than the nominal gateway-state
files for current activity.

## Driver / GPU Evidence

Fresh WSL-side sample:

- VRAM used:
  - `6328 MiB`
- GPU utilization:
  - `6 %`

Still modest and well below the earlier container-heavy baseline.

## New Conclusions Since Last Check-In

1. The revived Commander/Apollo API pair is still healthy enough to answer with
   `401 Unauthorized`.
2. No containerized inference or observability surfaces returned in this window.
3. Channel-directory files continue to be the most responsive activity signal in
   the legacy runtime.
4. Gateway-state files remain stale enough that they should not be treated as
   current truth.

## Recommended Next Moves

1. Keep treating channel-directory timestamps as one of the highest-value live
   indicators in the broken stack.
2. Keep separating "auth-enforcing local API is alive" from "runtime state files
   are accurate" because the evidence continues to show those are different.
3. Continue treating the container stack and the revived local control plane as
   separate runtime modes, since only one of them is alive at a time in current
   observations.
