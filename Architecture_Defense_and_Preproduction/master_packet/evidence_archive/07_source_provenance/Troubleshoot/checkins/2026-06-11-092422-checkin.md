# WSL Stack Check-In

- Timestamp: 2026-06-11T14:24:22.004Z
- Scope: Hermes persistence, scheduled-task evidence, stale gateway state, and current port reality

## Summary

This pass adds a strong operational explanation for the current “Hermes is supposed to be up, but it is dead” state:

- there are scheduled tasks for both Commander and Apollo gateway seats
- both last failed with `4294967295`
- the top-level Hermes gateway state file still claims a gateway is running
- the PID recorded there is stale and not actually alive

That means the control plane is carrying stale “running” state after failed or partial launches. Combined with prior duplicate Telegram polling evidence, this makes the control-plane layer look increasingly brittle and self-contradictory.

## New Evidence Gathered

### Scheduled tasks exist for multiple gateway seats

`Get-ScheduledTask` shows:

- `Hermes_Gateway_commander` -> `Ready`
- `Hermes_Gateway_apollo` -> `Ready`
- `Hermes_Gateway_bridge` -> `Disabled`

There are also several disabled profile-specific gateway tasks for supporting roles.

This matters because:

- Commander and Apollo are both configured as persistent scheduled gateway seats
- duplicate behavior no longer has to come only from manual relaunches
- automation and scheduled background startup are part of the topology

### Commander and Apollo scheduled tasks both failed last run

For both active scheduled tasks:

- `LastRunTime` = `2026-06-10 1:19:50 AM`
- `LastTaskResult` = `4294967295`

That means the scheduled service path is not currently healthy.

The task actions point to:

- `C:\Users\larry\.hermes\profiles\commander\gateway-service\Hermes_Gateway_commander.cmd`
- `C:\Users\larry\.hermes\profiles\apollo\gateway-service\Hermes_Gateway_apollo.cmd`

The bridge service task exists but is disabled.

### Gateway state file is stale

`C:\Users\larry\.hermes\gateway_state.json` currently says:

- `pid = 94652`
- `gateway_state = running`
- `platforms.api_server.state = retrying`
- `error_message = failed to reconnect`
- `updated_at = 2026-06-09T22:54:38.902609+00:00`

But a live process check shows:

- PID `94652` is **not running**

So the machine is carrying stale state that still advertises an active gateway after the process has already died.

This is important because it can mislead:

- scripts
- status checks
- the operator
- any logic that assumes “state file says running” means “runtime is healthy”

### Current live ports still contradict the intended control plane

Current port checks still show:

- `3000` -> `200`
- `11434` -> `200`
- `8092` -> reachable (`404` at root path, which still indicates a live HTTP service)
- `8642` -> refused
- `8643` -> refused
- `8644` -> refused
- `8645` -> refused
- `8646` -> refused
- `8089` -> refused
- `9119` -> refused

This means:

- the scheduled gateways are not restoring the expected Hermes surfaces right now
- the machine is still effectively operating on the Docker/Open WebUI/Ollama side while the documented control-plane APIs remain absent

### Gateway stderr contains repeated port-collision history on `:8642`

`C:\Users\larry\.hermes\gateway-stderr.log` contains many repetitions of:

- `Port 8642 already in use`

That supports a past failure mode where multiple launcher paths were colliding on the same intended API server port.

Important nuance:

- today, `:8642` is not listening
- historically, repeated attempts were failing because something else already had the port

This fits a pattern of overlapping starts, partial shutdowns, and stale service state.

### Gateway exit diagnostics show many starts, not a single stable daemon

`gateway-exit-diag.log` records many `gateway.start` events across multiple dates and PIDs, including:

- `hermes gateway run`
- `hermes ... gateway run --accept-hooks`
- `hermes_cli.main.py gateway`
- `hermes_cli.main.py gateway run`

This again suggests the control plane has been started through several mechanisms over time:

- direct CLI runs
- `--accept-hooks` runs
- likely service/task wrappers

That multi-path bring-up makes duplicate sessions and stale state more likely.

## Updated Working Hypotheses

### 1. Hermes control-plane duplication and stale service state are major root causes

Confidence increased again.

Why:

- scheduled tasks exist for multiple profile gateways
- duplicate Telegram polling is directly logged
- repeated `8642` port-collision history exists
- gateway state can remain “running” after the process is gone

This is now one of the strongest findings in the whole investigation.

### 2. Current Hermes outage is not just “it was never started”

Confidence increased.

Why:

- scheduled tasks did try to run
- logs show prior successful starts
- current state files imply retry behavior
- current live ports are still dead

So the failure mode is more like:

- attempted start
- unstable or conflicting runtime
- stale success/running markers left behind

### 3. The split stack is now explained by both intentional architecture and failed restoration

Confidence increased.

Interpretation:

- intentional: Hermes control plane uses OpenRouter/DeepSeek, local Gemma is separate
- failed restoration: the Hermes scheduled/control surfaces are not actually surviving startup today

### 4. GPU/driver theory continues to fall behind the evidence

Confidence decreased further as primary hypothesis.

Nothing in this pass added new driver evidence.
Everything new pointed to:

- service lifecycle failure
- duplicate launch paths
- stale state
- port collisions

## Open Questions

1. What exactly do the Commander and Apollo `gateway-service` `.cmd` wrappers do, and do they launch overlapping seats or shared resources?
2. Why did both scheduled tasks fail on the same last run timestamp?
3. Was a port collision on `:8642` caused by a second Hermes copy, another service, or a stale half-shutdown?
4. Is there a clean distinction between top-level `C:\Users\larry\.hermes\gateway_state.json` and per-profile runtime state, or are they drifting independently?

## Best Current Read

The stack now looks like it has three overlapping classes of failure:

1. **control-plane lifecycle instability**
   - scheduled tasks fail
   - ports collide
   - stale running state remains

2. **duplicate communications surfaces**
   - Telegram polling conflicts
   - likely overlapping gateway instances
   - cron + live delivery paths

3. **data/session/path hygiene failures**
   - session reuse bug in browser lane
   - recursive Site Studio import tree

This combination explains far more of the observed behavior than a hardware-only story.

## Next High-Value Checks

1. Read the `gateway-service` `.cmd` files for Commander and Apollo to see exactly how they launch and whether they compete for shared state.
2. Trace which config currently controls port `8642` vs `8645` and why runtime drift occurred.
3. Compare Commander and Apollo profile configs for overlapping Telegram or API bindings.
4. Continue the Site Studio recursion trace so we can tell whether that bug is still active or is a historical artifact.

## Context-Bloat Test Note

This third check-in adds genuinely new evidence:

- scheduled-task persistence for multiple seats
- failed scheduled-task results
- stale gateway PID proof
- repeated `8642` port-collision history

That should help evaluate whether the check-ins are continuing to produce new signal rather than just rephrasing prior conclusions.
