# WSL Stack Check-In

- Timestamp: 2026-06-11T10:24:22-05:00
- Automation: `wsl-stack-check-in`
- Investigator: Northstar (debug/rebuild mode)

## Summary

The fresh pass strengthens two major conclusions:

1. The Hermes control plane is still lying about health through stale state.
2. The `site-studio` recursive import problem is not hypothetical; the project
   tree is already deeply self-nested on disk.

The GPU/local inference path still looks healthier than the orchestration path.

## Runtime State

### Live listeners

- `3000` Open WebUI
- `3001` Grafana
- `8092` `anvil-26b-server`
- `9090` Prometheus
- `11434` native Ollama

### Still down

- `3006` Jarvis dashboard
- `3210` Site Studio UI
- `8089` documented Anvil lane
- `8642` Hermes main API
- `8643` Hermes bridge API
- `8644` Commander API
- `8646` Apollo API
- `9119` Hermes browser cockpit

### Containers

- `anvil-26b-server` -> healthy container on `8092`
- `gemma4-open-webui` -> healthy container on `3000`
- `onyx-prometheus` -> healthy container on `9090`
- `onyx-grafana` -> healthy container on `3001`

## Fresh Evidence

### Gateway state remains stale but now more specifically proven

Commander:

- `gateway.pid` JSON points at dead PID `30068`
- `gateway_state.json` still claims:
  - `gateway_state: running`
  - `api_server: connected`
  - `telegram: connected`

Apollo:

- `gateway.pid` JSON points at dead PID `30044`
- `gateway_state.json` still claims:
  - `gateway_state: running`
  - `api_server: connected`
  - `telegram: disconnected`

This means the legacy Hermes state layer is not simply stale at startup; it can
persist a fully healthy-looking runtime snapshot for dead processes.

### Session store still looks old and sticky

`$USER_HOME\.hermes\profiles\commander\sessions\sessions.json` contains old
Telegram-bound sessions from 2026-06-03, including:

- `agent:main:telegram:dm:479877962`
- `agent:main:telegram:group:-1003858086422:479877962`

Notable traits:

- `expiry_finalized: true`
- `is_fresh_reset: false`
- `was_auto_reset: false`

This does not directly prove browser session reuse by itself, but it supports
the broader pattern that session state is sticky, durable, and not being
aggressively normalized.

### Recursive workspace duplication is real and severe

Under:

- `L:\WSL\site-studio\projects\project-daf03eb0-20260518-061623`

there is a nested path:

- `...\WSL\site-studio\projects\project-daf03eb0-20260518-061623\WSL\site-studio\projects\project-daf03eb0-20260518-061623`

and further nesting below that again.

Count observed:

- `440` matching nested directories under the recursive project tree

This is strong filesystem proof that the import/copy behavior copied a managed
project tree back into itself or into a source that already contained that tree.

### Import code still looks compatible with the failure mode

`L:\WSL\site-studio\app.py` still shows:

- `shutil.copytree(source, dest, ignore=ignore)`
- `normalize_import_source(...)`
- `import_project(...)`

No contrary evidence was found during this pass that would rule out the import
logic as the source of the recursive duplication.

### Telegram/OpenRouter evidence remains unchanged but still relevant

Persistent historical proof remains in logs for:

- Telegram polling conflicts
- port collisions
- OpenRouter `429` throttling / upstream rate limiting

No new live runtime evidence suggests those issues were resolved.

## New Conclusions Since Last Check-In

1. The stale Hermes state problem is worse than “dead PID left behind”; the JSON
   state files still advertise healthy connected platforms for dead gateways.
2. The recursive `site-studio` project tree is severe enough to treat the
   project/import path as contaminated evidence, not a trusted workspace.
3. The session store still looks durable and sticky enough to remain compatible
   with the earlier browser session reuse diagnosis.
4. The healthiest running surfaces remain the local model and observability
   containers, not the control plane.

## Current Root-Cause Ranking

1. Broken control-plane ownership and stale-state truthfulness
2. Duplicate gateway/control-plane copies and Telegram polling conflicts
3. Recursive workspace/path duplication in `site-studio`
4. Sticky session reuse / poor session lifecycle normalization
5. Port drift and API surface inconsistency
6. OpenRouter throttling amplified by orchestration behavior
7. GPU/driver issue as a secondary concern rather than the leading cause

## GPU / Driver Read

No fresh evidence from this pass suggests the GPU path is the main break:

- local CUDA-backed lane on `8092` is still alive
- Open WebUI and Ollama remain reachable
- the dead surfaces continue to cluster around Hermes/Jarvis/control-plane state

That does not fully clear GPU/driver concerns for future benchmarking, but it
does keep the current primary blame on orchestration and state management.



