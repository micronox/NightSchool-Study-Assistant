# WSL Stack Check-In

- Timestamp: 2026-06-11T14:24:26-05:00
- Automation: `wsl-stack-check-in`
- Investigator: Northstar (debug/rebuild mode)

## Summary

This pass reinforces the idea that Commander became the stack's operational
gravity well while Apollo retained more of the conceptual/doctrinal identity.

Commander continues to hold:

- routing ownership
- cron execution
- mixed desktop sessions

while Commander and Apollo also share very similar memory file structure and
timestamps, which suggests some memory-state setup was duplicated even as the
operational surfaces diverged.

## Runtime State

### Still alive

- `3000` Open WebUI
- `3001` Grafana
- `8092` `anvil-26b-server`
- `9090` Prometheus
- `11434` native Ollama

### Still absent

- `3006`, `3210`, `8089`, `8642`, `8643`, `8644`, `8645`, `8646`, `9119`

## Fresh Evidence

### Commander and Apollo memory footprints are structurally very similar

Observed under profile memories:

Commander:

- `MEMORY.md`
- `MEMORY.md.lock`
- `USER.md`

Apollo:

- `MEMORY.md`
- `USER.md`

Interesting alignment:

- both `MEMORY.md` files have the same length
- both `MEMORY.md` files share the same timestamp:
  - `2026-06-03 11:12:31 PM`
- both `USER.md` files were updated the same day:
  - `2026-06-06 06:03:33 PM`

Interpretation:

- some memory setup or synchronization behavior was likely mirrored across
  profiles
- that does not by itself prove bad cross-writing, but it supports the broader
  pattern that the stack often duplicated state structures while failing to
  preserve clean operational separation

### Apollo memory content still preserves doctrinal identity

Apollo memory files still state clean conceptual boundaries such as:

- Apollo and Commander are peers under operator
- Commander governs machine/runtime/GPU posture
- Apollo governs canon, provenance, and infrastructure discipline
- Apollo Mercury Ops is a Telegram group

Interpretation:

- Apollo's saved memory still describes the intended architecture clearly
- this makes the failure even more likely to be operational drift rather than a
  total loss of conceptual design

### Commander operational records continue to contradict clean boundary intent

Commander still holds evidence of:

- Telegram routing ownership
- cron delivery ownership
- mixed desktop session backlog across many sources
- Apollo-themed scheduled work delivery

This continues the pattern:

- Apollo stores cleaner doctrine
- Commander accumulates execution surfaces

That asymmetry is now one of the defining observations of the system.

### Jarvis still encodes the intended lane separation more clearly than runtime

Jarvis dashboard code and tests still model:

- separated lanes
- bridge launcher surfaces
- separate `stinger` and `hermes` views
- cleaner launchpad boundaries

That means three different layers now tell different stories:

1. Apollo memory:
   - clean conceptual role boundaries
2. Jarvis/dashboard code:
   - clean intended lane/launchpad boundaries
3. live Commander runtime/state:
   - mixed operational accumulation

This is a strong sign that the system degraded through implementation/runtime
gravity, not because the architecture was never defined.

## New Conclusions Since Last Check-In

1. The conceptual framework remained legible in Apollo memory even while the
   live system drifted.
2. Commander and Apollo likely received mirrored memory-state scaffolding, but
   only Commander became the dominant operational sink.
3. The stack now looks like it split into:
   - doctrine living in Apollo/Onyx artifacts
   - reality collapsing into Commander runtime state
4. This gap between conceptual architecture and operational gravity is now one
   of the clearest explanations for why “nothing works” despite a strong design
   language.

## Current Root-Cause Ranking

1. Operational gravity pulling multiple lanes into Commander
2. Ownership drift between documented roles and actual runtime surfaces
3. Shared runtime drift plus dirty update/launch lifecycle in `hermes-agent`
4. Scheduler/launcher ownership failure for profile gateways
5. Stale profile runtime truth (`gateway.pid` / `gateway_state.json`)
6. Duplicate gateway/control-plane copies and Telegram polling conflicts
7. Recursive workspace duplication in `site-studio`
8. Browser `/chat` session reuse through `claude-last-session`
9. Inconsistent local API probing / bridge expectations
10. OpenRouter throttling amplified by orchestration behavior
11. GPU/driver issue as a secondary concern rather than the leading cause

## GPU / Driver Read

No fresh GPU-negative evidence appeared during this pass.

The evidence continues to concentrate on runtime ownership, memory structure,
and lane-boundary collapse rather than on GPU execution failure.


