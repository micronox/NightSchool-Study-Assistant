# Check-In 2026-06-13 05:39:48

- Automation: `wsl-stack-check-in`
- Scope: validation of revived Commander/Apollo API surfaces and fresh runtime
  activity signals

## Executive Delta

The revived control plane is confirmed to be real, not just listening sockets.

Both Commander and Apollo are actively serving API responses on localhost:

- Commander on `8645`
- Apollo on `8646`

Unauthenticated requests to `/v1/models` on both ports return:

- `Invalid API key`

That means the local APIs are alive enough to parse requests and enforce auth.

## API Surface Evidence

### Commander

Request:

- `http://127.0.0.1:8645/v1/models`

Response:

- error type:
  - `invalid_request_error`
- code:
  - `invalid_api_key`

### Apollo

Request:

- `http://127.0.0.1:8646/v1/models`

Response:

- error type:
  - `invalid_request_error`
- code:
  - `invalid_api_key`

### Interpretation

This confirms:

- both local API servers are actively running
- both are enforcing auth
- the revived control plane is operational enough to accept and process requests

## Fresh File Activity

Current file timestamps show ongoing runtime-side updates:

### Gateway state files

- Commander `gateway_state.json`
  - still `2026-06-12 11:11:31 AM`
- Apollo `gateway_state.json`
  - still `2026-06-12 11:11:10 AM`

These did not move this round.

### Channel directories

- Commander `channel_directory.json`
  - `2026-06-13 5:35:53 AM`
- Apollo `channel_directory.json`
  - `2026-06-13 5:35:32 AM`

These did move recently, so the routing layer remains active enough to mutate
channel metadata even when `gateway_state.json` is not being refreshed at the
same cadence.

## Commander Runtime Error Signals

Commander `gateway-stderr.log` continues to show active operational instability.

### Telegram issues

- repeated Telegram reconnect failures
- `telegram.error.TimedOut`
- `httpx.ReadError`
- fallback IP attempts to Telegram
- DNS/connectivity failures such as:
  - `getaddrinfo failed`
  - `All connection attempts failed`

### MCP issues

- repeated failure connecting to MCP server `higgsfield`
- initial connection failed after 3 attempts
- session terminated

### OpenRouter issues

- stream drop / network loss warnings
- explicit `HTTP 429`
- upstream rate limit for:
  - `nvidia/nemotron-3-super-120b-a12b`

This is useful because it links the revived Commander runtime to exactly the
kind of external-friction behavior that could create bursty retries, message
chatter, or unstable session behavior.

## Apollo Runtime Signal

Apollo stderr was much quieter in this sample.

Observed warnings were mostly about:

- no allowlists configured
- missing Nous authentication
- auxiliary provider health being marked unhealthy

This suggests Commander is still the noisier operational gravity well.

## Driver / GPU Evidence

Fresh WSL-side sample:

- VRAM used:
  - `7612 MiB`
- GPU utilization:
  - `9 %`

This is still far below the earlier `~26 GB` baseline from the container-heavy
state on June 11.

## Interpretation

### Strong conclusion

The live state is now better characterized as:

- local Hermes control plane alive
- containerized inference stack still down
- Commander experiencing active external-service instability
- Apollo quieter but still live

### Why this matters

The stack's failures are not just "it crashes" or "it is down."

They include:

- live local APIs
- stale or uneven state-file refresh
- Telegram transport instability
- MCP connection failures
- OpenRouter upstream throttling

That combination can plausibly create the kind of unpredictable chatter,
session weirdness, and retry storms you described earlier.

## New Conclusions Since Last Check-In

1. Commander and Apollo APIs are definitely live and enforcing authentication.
2. Commander remains the highest-noise runtime surface.
3. Fresh channel-directory mutations combined with stale gateway-state timestamps
   suggest uneven runtime bookkeeping.
4. OpenRouter throttling and Telegram transport failures are present in the live
   Commander error stream.

## Recommended Next Moves

1. Preserve Commander stderr as primary evidence for burst/retry behavior.
2. Continue treating Commander as the main suspect for runaway operational
   chatter.
3. Keep separating:
   - local runtime health
   - external transport health
   - model-provider throttling
   because all three are failing in different ways.
