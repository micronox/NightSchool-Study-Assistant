# Check-In 2026-06-11 15:24:39

- Automation: `wsl-stack-check-in`
- Scope: local Hermes/Apollo/Anvil/Onyx stack heartbeat refresh after Terran
  peer-review incorporation

## Current Findings

The broad runtime state is unchanged, but one important improvement happened in
the rebuild scaffold itself: duplicate Telegram token ownership is no longer
just an inference from logs. It is now machine-detectable in the new control
plane preflight.

### Live lanes and ports

Still listening:

- `3000`
  - Open WebUI
- `3001`
  - Grafana
- `8092`
  - `anvil-26b-server`
- `9090`
  - Prometheus
- `11434`
  - native Ollama

Still not listening:

- `3006`
- `3210`
- `8012`
- `8013`
- `8089`
- `8642`
- `8643`
- `8644`
- `8645`
- `8646`
- `9102`
- `9119`
- `11435`

### Containers

Still up:

- `anvil-26b-server`
- `onyx-prometheus`
- `onyx-grafana`
- `gemma4-open-webui`

This continues to support the finding that the most stable surfaces are local
inference plus observability, not the Hermes control plane.

## New Evidence

### Duplicate token ownership is now explicitly detected

The updated health script under:

- `L:\AI-Stack\ops\scripts\test-lane-health.ps1`

now reports:

- `token_ref`: `telegram.primary`
- `profile_ids`: `apollo, commander`
- issue:
  - `Multiple Telegram-enabled profiles reference the same token.`

This is one of the strongest concrete control-plane findings in the entire
investigation because it upgrades the duplicate-poller theory into an enforced
contract violation in the rebuild scaffold.

### Port nuance around Ollama

Listener sampling now shows:

- `11434` owned by Windows `ollama` process
- `11434` also visible on `127.0.0.1` through another PID path associated with
  the container/WSL networking layer

This is not yet proof of a fault by itself, but it reinforces the need to treat
port ownership carefully in a mixed Windows + WSL + Docker environment. Local
service activation can appear from more than one side of the bridge.

## Driver / GPU / WSL Evidence

### GPU sample

Current WSL-side sample:

- GPU:
  - `NVIDIA RTX PRO 6000 Blackwell Workstation Edition`
- driver:
  - `610.47`
- VRAM used:
  - `26075 MiB`
- VRAM total:
  - `97887 MiB`
- GPU utilization:
  - `2 %`

### `nvtop`

Confirmed present:

- `/usr/bin/nvtop`

One command emitted a Windows-side `readlink` error while still confirming
`NVTOP=/usr/bin/nvtop`. This was a command-construction artifact, not evidence
that `nvtop` is broken.

## Suspected Root Causes

### Highest-confidence

- duplicate gateway/control-plane ownership
- stale runtime truth after failed launches
- session/state contamination across Commander/Hermes/Apollo
- recursive workspace/path duplication in `site-studio`
- docs/runtime/port drift

### Hitch contributors still under observation

- WSL2 memory growth and host-side remap behavior
- Docker Desktop adjacency
- mixed Windows/WSL exposure of local services
- high baseline VRAM residency before focused inference tests

## New Conclusions Since Last Check-In

1. The rebuild scaffold is improving faster than the legacy runtime.
   - Health checks now detect token collisions directly.
   - Registry-driven port inventory is now available.

2. The duplicate Telegram owner problem is confirmed at contract level, not just
   by runtime logs.
   - Commander and Apollo both still claim the same symbolic token reference.

3. No live recovery has occurred in the legacy control plane.
   - The same stable model and observability services remain up.
   - The same intended control-plane surfaces remain absent.

4. The future clean external lane remains the correct next technical step.
   - `8012` is still free and documented for the bare-metal `llama.cpp` lane.

## Recommended Next Moves

1. Continue leaving the current Onyx stack untouched during the external lane
   bring-up.
2. Use the external `llama.cpp` lane on `8012` as the first clean activation
   boundary.
3. After the external lane is alive, scaffold the separate Terran external
   profile around it, outside the Onyx runtime and Telegram ownership surfaces.
