# Check-In 2026-06-13 06:39:47

- Automation: `wsl-stack-check-in`
- Scope: compare live runtime against rebuild registry and track which state
  surfaces are still moving

## Executive Delta

The live Commander/Apollo runtime remains stable across the last 30 minutes, but
the mismatch between "what is alive" and "what the state files say" is getting
more precise.

The key pattern now is:

- live APIs on `8645` and `8646`
- live long-running Python gateway processes
- channel directories continuing to update this morning
- gateway state files frozen at June 12
- `processes.json` present but still literally `[]`

That is a strong example of a runtime that is operating without maintaining a
coherent internal ledger.

## Live Runtime Snapshot

### Still listening

- `8645`
  - Commander
  - PID `30756`
- `8646`
  - Apollo
  - PID `30732`
- `11434`
  - Ollama
  - PID `66340`

### Still not listening

- `8643`
- `8644`
- `3000`
- `3001`
- `8092`
- `9090`

The revived local control plane is still up while the containerized inference
and observability surfaces remain down.

## Process Runtime Evidence

Live process sample:

- Commander gateway PID `30756`
  - CPU time: `294.81`
  - started: `June 12, 2026 11:10:09 AM`
- Apollo gateway PID `30732`
  - CPU time: `267.33`
  - started: `June 12, 2026 11:10:09 AM`
- Ollama PID `66340`
  - CPU time: `3.94`
  - started: `June 12, 2026 11:18:43 AM`

The gateway PIDs continue accumulating CPU time, which supports the conclusion
that they are genuinely active, not just stranded listeners.

## Registry vs Live-State Drift

The rebuild health script still reports:

- Commander expected on `8644`
  - not listening
- Apollo expected on `8646`
  - listening

Duplicate token finding still present:

- `telegram.primary`
  - `apollo, commander`

This means two drift layers remain active at once:

1. port drift
2. token-ownership drift

## File-Ledger Drift

### Frozen state files

- Commander `gateway_state.json`
  - `2026-06-12 11:11:31 AM`
- Apollo `gateway_state.json`
  - `2026-06-12 11:11:10 AM`

### Fresh channel files

- Commander `channel_directory.json`
  - `2026-06-13 6:35:55 AM`
- Apollo `channel_directory.json`
  - `2026-06-13 6:35:33 AM`

### Empty process ledger

- Commander `processes.json`
  - file timestamp: `2026-06-12 11:11:27 AM`
  - actual content: `[]`

### Interpretation

This is now a repeatable, evidence-backed pattern:

- live runtime activity is visible from ports and process CPU time
- routing metadata continues to advance
- state and process bookkeeping are either stale or empty

That makes the live stack operationally active but epistemically weak.

## Driver / GPU Evidence

Fresh WSL-side sample:

- VRAM used:
  - `8115 MiB`
- GPU utilization:
  - `7 %`

Still modest and stable compared with the earlier container-heavy posture.

## New Conclusions Since Last Check-In

1. The revived control plane is persisting, not flapping minute-to-minute.
2. Commander and Apollo gateway processes are still doing real work as shown by
   accumulated CPU time.
3. `processes.json` is not a trustworthy source of activity state.
   - it exists
   - it is timestamped
   - it still contains `[]`
4. Channel-directory updates are currently one of the most trustworthy clues for
   live routing behavior.

## Recommended Next Moves

1. Treat channel-directory timestamps plus live port/process checks as the best
   current "truth trio" for legacy runtime activity.
2. Continue treating `gateway_state.json` and `processes.json` as low-trust
   bookkeeping surfaces.
3. Keep the rebuild registry explicitly marked as target-state truth, not live
   truth, until cutover is complete.
