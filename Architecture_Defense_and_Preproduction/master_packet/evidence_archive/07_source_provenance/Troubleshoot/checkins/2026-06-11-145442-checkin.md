# Check-In 2026-06-11 14:54:42

- Automation: `wsl-stack-check-in`
- Scope: local Hermes/Apollo/Anvil/Onyx stack heartbeat refresh

## Current Findings

The runtime picture remains consistent with the earlier diagnosis: the healthy
surfaces are still the inference and observability lanes, while the broken
surfaces remain clustered around the Hermes control plane and duplicated gateway
ownership.

### Live listeners

Confirmed listening now:

- `3000`
  - Open WebUI
- `3001`
  - Grafana
- `8092`
  - `anvil-26b-server`
- `9090`
  - Prometheus
- `11434`
  - native Ollama

No new listeners appeared on the historically drifted control-plane ports:

- `3006`
- `3210`
- `8089`
- `8642`
- `8643`
- `8644`
- `8645`
- `8646`
- `9119`

### Containers

Still up:

- `anvil-26b-server`
- `onyx-prometheus`
- `onyx-grafana`
- `gemma4-open-webui`

Container uptime remains long-lived and stable, which continues to support the
finding that the inference/observability layer is healthier than the
Commander/Apollo control plane.

### Scheduled gateway state

Gateway task states remain:

- `Hermes_Gateway_commander`
  - `Ready`
- `Hermes_Gateway_apollo`
  - `Ready`

But their scheduler metadata remains stale and failed:

- Commander:
  - `LastRunTime`: `2026-06-10 01:19:50`
  - `LastTaskResult`: `4294967295`
  - `NextRunTime`: blank
- Apollo:
  - `LastRunTime`: `2026-06-10 01:19:50`
  - `LastTaskResult`: `4294967295`
  - `NextRunTime`: blank

This is still strong evidence that the two major gateway owners share a broken
launch/update path rather than suffering isolated profile-specific failures.

## Driver / GPU / WSL Evidence

### WSL and GPU

Inside the `PopOS` WSL instance:

- `/etc/os-release` still reports:
  - `PRETTY_NAME="Ubuntu 24.04.4 LTS"`
- `nvidia-smi` reports:
  - GPU: `NVIDIA RTX PRO 6000 Blackwell Workstation Edition`
  - driver: `610.47`
  - VRAM used at sample time: `25988 MiB`
  - VRAM total: `97887 MiB`
  - GPU utilization: `5 %`

### `nvtop`

`nvtop` is installed and present at:

- `/usr/bin/nvtop`

One noninteractive probe returned `NVTOP=` with no path, but a direct path check
confirmed the binary exists. Most likely explanation: command formatting or PATH
resolution difference in the one-liner probe, not an actual missing install.

### Interpretation

This matters because it keeps pointing away from "missing GPU tooling" and back
toward activation-path behavior:

- WSL is up
- containers are up
- GPU bridge is visible
- inference surfaces are up
- hitching is still more plausibly caused by orchestration wakeup, memory remap,
  or duplicated runtime activation than by lack of raw hardware

## Suspected Root Causes

No root-cause ranking change from the last pass.

### Highest-confidence causes

- duplicate gateway/control-plane ownership
- stale scheduler/runtime truth after failed launches
- session/state contamination across Commander/Hermes/Apollo boundaries
- recursive workspace/path duplication in `site-studio`
- documentation/runtime/port drift

### Secondary but important hitch contributors

- WSL2 memory pressure and `vmmemWSL` growth during activation
- Docker Desktop wake and container adjacency
- high baseline VRAM residency on the workstation before focused tests
- host-side memory mapping or paging behavior during model load

## New Conclusions Since Last Check-In

1. The live runtime has not materially changed.
   - No new control-plane surfaces recovered.
   - No new ports appeared.
   - The stable surfaces remain stable.

2. The shared gateway failure timestamp still anchors the control-plane
   diagnosis.
   - Commander and Apollo continue to look like two owners hanging off one
     broken runtime/update path.

3. `nvtop` is now verified installed in WSL.
   - GPU-side observation inside the distro is available for upcoming A/B tests.

4. The rebuild brief and external `llama.cpp` lane runbook are now the most
   useful durable handoff artifacts for the next phase.

## Recommended Next Moves

1. Keep the current Onyx stack unchanged while building the external bare-metal
   `llama.cpp` lane.
2. Use the new external lane to isolate:
   - `llama.cpp` only
   - fresh Hermes only
   - no Docker
   - no Telegram
   - no Onyx shared profile state
3. Use `nvidia-smi`, `nvtop`, Task Manager, and `vmmemWSL` during activation to
   identify the exact hitch boundary.
