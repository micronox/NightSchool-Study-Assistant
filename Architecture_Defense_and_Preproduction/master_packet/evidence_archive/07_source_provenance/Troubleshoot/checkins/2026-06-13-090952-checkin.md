# Check-In 2026-06-13 09:09:52

- Automation: `wsl-stack-check-in`
- Scope: additional persistence check on the sustained Commander/Apollo/Ollama
  mixed mode

## Executive Delta

No material runtime transition occurred in this interval.

The same three live surfaces remain:

- Commander on `8645`
- Apollo on `8646`
- Ollama on `11434`

The same containerized surfaces remain absent:

- `3000`
- `3001`
- `8092`
- `9090`

The same bookkeeping split remains:

- channel-directory files keep updating
- gateway-state files stay frozen on June 12

This repetition is useful because it turns the earlier observations into a very
stable pattern rather than isolated snapshots.

## Live Runtime Snapshot

### Listening ports

- `8645`
  - PID `30756`
- `8646`
  - PID `30732`
- `11434`
  - PID `66340`

### CPU accumulation

Current totals:

- Commander gateway PID `30756`
  - `336.27`
- Apollo gateway PID `30732`
  - `299.28`
- Ollama PID `66340`
  - `4.14`

Both gateway processes continue to accumulate CPU time, confirming sustained
process activity.

## Routing vs State Drift

### Fresh channel-directory writes

- Commander `channel_directory.json`
  - `2026-06-13 9:04:58 AM`
- Apollo `channel_directory.json`
  - `2026-06-13 9:05:36 AM`

### Frozen gateway-state files

- Commander `gateway_state.json`
  - `2026-06-12 11:11:31 AM`
- Apollo `gateway_state.json`
  - `2026-06-12 11:11:10 AM`

### Interpretation

The routing layer is still the most actively refreshed part of the legacy
runtime, while the nominal gateway-state layer remains stale.

## Container Status

`docker ps` still reports no running containers.

This continues to support the current two-mode interpretation:

- containerized inference/observability mode: down
- revived local Commander/Apollo control-plane mode: up

## Driver / GPU Evidence

Fresh WSL-side sample:

- VRAM used:
  - `6237 MiB`
- GPU utilization:
  - `4 %`

Still low and stable.

## New Conclusions Since Last Check-In

1. The sustained mixed mode remains extremely stable across repeated hourly
   checks.
2. Channel-directory hourly updates have now become one of the most repeatable
   runtime signatures in the legacy stack.
3. The absence of containers is still a durable state, not an intermittent
   outage.
4. GPU pressure remains low while the local control plane stays alive.

## Recommended Next Moves

1. Continue preserving this repeated pattern as evidence that the legacy stack
   can remain operationally active while internally incoherent.
2. Keep the routing-layer timestamps front and center in the final incident
   reconstruction.
3. Preserve the current live-port trio (`8645`, `8646`, `11434`) as the
   definitive current-state runtime map until the next material change.
