# Check-In 2026-06-13 07:39:48

- Automation: `wsl-stack-check-in`
- Scope: persistence check on revived Commander/Apollo runtime and unresolved
  control-plane risks

## Executive Delta

The revived local control plane continues to persist without major change:

- Commander still live on `8645`
- Apollo still live on `8646`
- Ollama still live on `11434`
- duplicate token finding still unresolved
- channel-directory files continue to advance
- gateway-state files remain frozen on June 12

This means the current state is not a brief transient. It is a sustained mixed
mode.

## Persistence Evidence

### Live ports

Still listening:

- `8645`
  - Commander
  - PID `30756`
- `8646`
  - Apollo
  - PID `30732`
- `11434`
  - Ollama
  - PID `66340`

### Process activity

Current CPU totals:

- Commander gateway PID `30756`
  - `314.48`
- Apollo gateway PID `30732`
  - `280.00`
- Ollama PID `66340`
  - `4.05`

Compared with the prior pass, both gateway Python processes have continued to
accumulate CPU time, which is further evidence that they are doing ongoing work
rather than sitting idle.

## Control-Plane Risk Still Unresolved

The rebuild health script still flags:

- `token_ref`: `telegram.primary`
- `profile_ids`: `apollo, commander`

That keeps the duplicate-token collision as the clearest unresolved
control-plane defect in the current model.

Even if the current live runtime behavior is more nuanced than the symbolic
contract, this issue remains actionable because it identifies a boundary that
must be made explicit during rebuild.

## Bookkeeping Drift Still Active

### Fresh channel files

- Commander `channel_directory.json`
  - `2026-06-13 7:34:56 AM`
- Apollo `channel_directory.json`
  - `2026-06-13 7:35:34 AM`

### Frozen state files

- Commander `gateway_state.json`
  - `2026-06-12 11:11:31 AM`
- Apollo `gateway_state.json`
  - `2026-06-12 11:11:10 AM`

### Interpretation

The same split remains:

- routing metadata continues to update
- state bookkeeping remains stale

That is now a highly repeatable observation across multiple consecutive
check-ins.

## Container / Inference Status

Still absent:

- `3000`
- `3001`
- `8092`
- `9090`

So the system remains in:

- live local control plane mode
- no containerized inference/observability mode

## Driver / GPU Evidence

Fresh WSL-side sample:

- VRAM used:
  - `6330 MiB`
- GPU utilization:
  - `5 %`

Again low and stable.

## New Conclusions Since Last Check-In

1. The revived Commander/Apollo runtime is persisting over time rather than
   flapping.
2. The duplicate-token finding remains the most concrete unresolved
   control-plane contract issue.
3. Channel-directory timestamps continue to be the most responsive signal for
   live routing behavior.
4. Gateway-state timestamps remain too stale to treat as live truth.

## Recommended Next Moves

1. Continue treating this as a stable but low-trust mixed mode, not a recovery.
2. Preserve the duplicate-token finding as a primary cutover blocker.
3. Preserve the channel-directory vs. gateway-state mismatch as one of the core
   examples of why the legacy runtime cannot be trusted as its own source of
   truth.
