# WSL Stack Check-In

- Timestamp: 2026-06-11T09:54:22-05:00
- Automation: `wsl-stack-check-in`
- Investigator: Northstar (debug/rebuild mode)

## Summary

The strongest live evidence still points to a broken control plane, not a dead
GPU path. The local inference and observability containers are up and stable,
while the Hermes/Jarvis/profile APIs remain down. Commander and Apollo each
still carry stale dead gateway ownership in their profile state, which matches
the broader pattern of duplicate/unclean gateway lifecycle handling.

## Fresh Runtime State

### Live listeners

- `3000` Open WebUI
- `3001` Grafana
- `8092` `anvil-26b-server`
- `9090` Prometheus
- `11434` native Ollama

### Still not listening

- `3006` Jarvis dashboard
- `3210` Site Studio
- `8089` canonical Anvil lane from older Onyx docs
- `8642` Hermes main API
- `8643` Hermes bridge API
- `8644` Commander API
- `8646` Apollo API
- `9119` Hermes browser cockpit

### Active containers

- `anvil-26b-server` -> `8092`
- `gemma4-open-webui` -> `3000`
- `onyx-prometheus` -> `9090`
- `onyx-grafana` -> `3001`

## Fresh Evidence

### Gateway state remains stale and broken

- `C:\Users\larry\.hermes\profiles\commander\gateway.pid` contains JSON with
  dead PID `30068`
- `C:\Users\larry\.hermes\profiles\apollo\gateway.pid` contains JSON with dead
  PID `30044`
- This matters because the old runtime format is not a bare PID file. Any
  cleanup or watchdog logic that assumes a plain integer can misread state and
  leave the system believing ownership still exists.

### Duplicate Telegram polling remains proven

Both Commander and Apollo logs still contain repeated Telegram conflicts:

- `Conflict: terminated by other getUpdates request; make sure that only one bot instance is running`

This is still the clearest proof that gateway ownership was duplicated or not
cleanly released.

### Port collision history remains proven

Apollo logs still show:

- `Port 8645 already in use`

This reinforces the duplicate-start / unstable ownership theory.

### OpenRouter burst / throttle evidence remains real

Commander logs still show OpenRouter upstream rate limiting:

- HTTP `429`
- provider metadata indicating temporary upstream throttling
- retries against `nvidia/nemotron-3-super-120b-a12b`

This does not prove the exact Mercury-side 80k-token burst by itself, but it
does prove the stack was hitting throttling/rate-limit conditions under the
existing orchestration model.

### Session reuse remains implicated

No new contradictory evidence showed up against the earlier Commander session
reuse diagnosis. The current pass did not re-open the browser session mapping,
but nothing in the live runtime suggests that issue was resolved.

## New Conclusions Since Last Check-In

1. Commander and Apollo are not sharing one live gateway PID; instead, they
   each retain their own stale dead gateway record.
2. The legacy gateway state format itself drifted enough to break naive cleanup
   assumptions, which helps explain why stale ownership persisted.
3. The local model path is still the healthiest part of the stack:
   `8092`, `3000`, and `11434` are alive while the control-plane surfaces are
   absent.
4. The rebuild scaffold under `L:\AI-Stack` is now the safest source of future
   runtime truth because it already captures these failures explicitly.

## Current Root-Cause Ranking

1. Broken control-plane ownership and stale state cleanup
2. Duplicate Telegram pollers / duplicate gateway starts
3. Port drift and API surface inconsistency
4. Session reuse / context bloat in browser flows
5. Recursive workspace/path duplication in Site Studio
6. Provider throttling amplified by orchestration behavior
7. GPU or driver issue as a secondary concern, not the primary failure mode

## Recommended Immediate Next Moves

1. Back up legacy state before any destructive cleanup
2. Normalize stale Hermes gateway state using the new `AI-Stack` ops scripts
3. Bring back exactly one control-plane owner at a time
4. Use a strong local/near-local workhorse (`Qwen` or `DeepSeek v4`) to assist
   rebuild tasks without letting it become the truth owner
5. Keep Jarvis and the lane registry driven from the new scaffold, not the old
   drifted docs

