# Check-In 2026-06-13 04:39:51

- Automation: `wsl-stack-check-in`
- Scope: resumed local Hermes/Apollo/Anvil/Onyx stack audit after a gap in
  monitoring

## Executive Delta

The runtime state changed materially since the last check-in on June 11, 2026.

The system is no longer in the exact same "healthy inference / dead
control-plane" posture. Instead, it has shifted into a more dangerous mixed
state:

- Docker-backed inference and observability surfaces are down
- Ollama is still up
- Apollo control-plane API is now live on `8646`
- an additional listener appeared on `8645`
- both Commander and Apollo gateway scheduled tasks last reported success
- the duplicate Telegram token collision still exists in the rebuild health
  contract

This means the stack is not simply broken and inert. Parts of it revived, but
they revived into a still-ambiguous ownership model.

## Current Runtime State

### Listening ports observed now

- `8645`
  - `127.0.0.1`
  - PID `30756`
- `8646`
  - `127.0.0.1`
  - PID `30732`
- `11434`
  - Ollama
  - PID `66340`

### Previously stable surfaces now down

Not listening now:

- `3000`
  - Open WebUI
- `3001`
  - Grafana
- `8092`
  - `anvil-26b-server`
- `9090`
  - Prometheus

`docker ps` returned no running containers.

### Health script snapshot

Registry-driven health check now reports:

- `apollo-control`
  - `8646`
  - listening
- `commander-control`
  - `8644`
  - not listening
- `anvil-worker`
  - `8092`
  - not listening
- `openwebui-surface`
  - `3000`
  - not listening
- `observability-core`
  - `9090`, `3001`
  - not listening
- `ollama-utility`
  - `11434`
  - listening

## Gateway / Scheduler Evidence

### Scheduled task outcome changed

Both primary gateway tasks now show a successful run:

- `Hermes_Gateway_apollo`
  - `LastRunTime`: `June 12, 2026 11:09:01 AM`
  - `LastTaskResult`: `0`
- `Hermes_Gateway_commander`
  - `LastRunTime`: `June 12, 2026 11:09:01 AM`
  - `LastTaskResult`: `0`

This is a major change from the earlier shared failure timestamp on June 10.

### Process ownership

Observed process identities:

- PID `30732`
  - `python.exe`
  - started `June 12, 2026 11:10:09 AM`
- PID `30756`
  - `python.exe`
  - started `June 12, 2026 11:10:09 AM`
- PID `66340`
  - `ollama.exe`
  - started `June 12, 2026 11:18:43 AM`

The two Python processes started at the same second, consistent with paired
gateway/task startup.

## Runtime-State File Evidence

### Apollo state

`$USER_HOME\.hermes\profiles\apollo\gateway_state.json` currently reports:

- PID `30732`
- `gateway_state`: `running`
- `api_server.state`: `connected`
- `telegram.state`: `disconnected`
- `updated_at`: `2026-06-12T16:11:10Z`

### Commander state

`$USER_HOME\.hermes\profiles\commander\gateway_state.json` currently reports:

- PID `30756`
- `gateway_state`: `running`
- `api_server.state`: `connected`
- `telegram.state`: `connected`
- `updated_at`: `2026-06-12T16:11:31Z`

At least in this snapshot, the gateway state files are no longer obviously
lying about dead PIDs. They now match live processes.

## Persistent Control-Plane Violation

The duplicate token finding is still present in the rebuild health script:

- `token_ref`: `telegram.primary`
- `profile_ids`: `apollo, commander`

This matters even more now because the system is not just theoretically
duplicated. Two gateway tasks successfully launched on June 12 while the
contract still says both Telegram-enabled profiles point at the same token
reference.

The practical interpretation is:

- either the symbolic token reference is modeling a real live collision
- or the contract is ahead of the actual current config and needs refinement

Given the earlier Telegram `getUpdates` conflict evidence, the safer working
assumption remains that this is a real collision risk until disproven.

## Driver / GPU Evidence

Current WSL-side sample:

- GPU:
  - `NVIDIA RTX PRO 6000 Blackwell Workstation Edition`
- driver:
  - `610.47`
- VRAM used:
  - `5438 MiB`
- VRAM total:
  - `97887 MiB`
- GPU utilization:
  - `5 %`
- `nvtop` present:
  - `/usr/bin/nvtop`

This is a sharp drop from the ~26 GB baseline seen during the earlier June 11
checks. That change lines up with the Docker/container surfaces being down.

## Interpretation

### New top-level picture

The system appears to have transitioned from:

- active containerized inference + dead Hermes control plane

to:

- dead containerized inference + partially revived Hermes control plane

That suggests the current instability may involve mode switching between
competing runtime stacks rather than one single stack staying continuously up.

### Why this matters

This mixed-state behavior supports the larger rebuild thesis:

- the system lacks a single authoritative startup/ownership model
- different subsystems can come alive independently
- the resulting machine posture is inconsistent and hard to reason about

## New Conclusions Since Last Check-In

1. The control plane is no longer fully absent.
   - Apollo API is now live on `8646`.
   - a second live listener exists on `8645`.

2. Docker-backed inference and observability are currently down.
   - no containers were running at sample time.

3. Gateway state is temporarily more truthful than before.
   - live PIDs now align with `gateway_state.json`.

4. The duplicate-token risk remains unresolved.
   - the rebuilt health script still detects Commander/Apollo token overlap.

5. GPU pressure is currently low.
   - this further weakens the idea that raw hardware shortage is the central
     issue.

## Recommended Next Moves

1. Treat June 12 as a meaningful transition point in the incident timeline.
2. Capture what restarted the gateway tasks successfully on June 12 at
   `11:09 AM`.
3. Determine what `8645` represents in the revived state and whether it is tied
   to Commander, Apollo, or a bridge sidecar.
4. Continue to avoid trusting this revived mixed state as a stable recovery.
   - it is evidence, not a validated end-state


