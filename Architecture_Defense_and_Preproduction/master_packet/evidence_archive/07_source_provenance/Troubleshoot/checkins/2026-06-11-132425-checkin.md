# WSL Stack Check-In

- Timestamp: 2026-06-11T13:24:25-05:00
- Automation: `wsl-stack-check-in`
- Investigator: Northstar (debug/rebuild mode)

## Summary

This pass produced the clearest routing-state evidence yet that Commander became
the primary Telegram/channel sink.

Commander's `channel_directory.json` currently owns both:

- the direct DM channel
- the `Apollo Mercury Ops` Telegram group

Apollo's `channel_directory.json` is empty.

That strongly supports the theory that channel and bot-lane ownership drifted
into Commander even for surfaces that should have remained more isolated.

## Runtime State

### Still alive

- `3000` Open WebUI
- `3001` Grafana
- `8092` `anvil-26b-server`
- `9090` Prometheus
- `11434` native Ollama

### Still absent

- `3006`, `3210`, `8089`, `8642`, `8643`, `8644`, `8645`, `8646`, `9119`

## Fresh Evidence

### Commander currently owns the Telegram channel directory

`$USER_HOME\.hermes\profiles\commander\channel_directory.json` currently
lists Telegram entries for:

- `479877962` -> `KryptoKoolAid` (DM)
- `-1003858086422` -> `Apollo Mercury Ops` (group)

The file timestamped update window is:

- `updated_at: 2026-06-10T01:26:35`

Interpretation:

- Commander is the active persisted routing owner for both the direct channel
  and the Mercury-linked group
- this is not just inferred from logs anymore; it is explicit in the current
  saved routing directory

### Apollo currently has no Telegram channels in its directory

`$USER_HOME\.hermes\profiles\apollo\channel_directory.json` shows:

- `telegram: []`

This matters because Apollo's historical logs definitely show Apollo previously
connecting to Telegram and suffering polling conflicts.

Interpretation:

- Apollo once touched the Telegram lane
- current saved routing state says it no longer owns any Telegram channels
- this is hard evidence of ownership migration or cleanup drift over time

### Commander has a cron job configured to deliver via Telegram

Search results show:

- `$USER_HOME\.hermes\profiles\commander\cron\jobs.json`
  contains `deliver: "telegram"`

This reinforces the current Commander-as-transport-owner pattern:

- channel directory points at Commander
- cron delivery points at Commander
- Commander desktop ledger already mixes cron/telegram/api_server/cli/tui

Taken together, Commander looks like the stack's operational accumulator.

### Apollo process ledger is missing

Observed:

- `$USER_HOME\.hermes\profiles\commander\processes.json` exists as an empty
  array
- `$USER_HOME\.hermes\profiles\apollo\processes.json` does not exist

Interpretation:

- process-tracking symmetry between profiles is weak or broken
- not every profile persisted the same operational metadata surfaces

This is another example of low-trust runtime state consistency.

### Current docs/scripts still assume many richer live surfaces

The broader `L:\WSL` and `L:\Jarvis` materials still reference:

- `8642`, `8643`, `8644`, `8646`
- `3006`
- `8089`
- `/v1/models` health checks across those lanes

But the machine still only exposes:

- `3000`
- `3001`
- `8092`
- `9090`
- `11434`

This means the documented lane map and the current persisted routing state are
both now diverged from the real live surface.

## New Conclusions Since Last Check-In

1. Commander is now the clearest persisted owner of the Telegram routing layer.
2. `Apollo Mercury Ops` being present in Commander's routing directory is strong
   evidence that Mercury/Apollo-linked traffic got folded into Commander state.
3. Apollo's empty routing directory, despite its historical Telegram conflicts,
   suggests ownership drift or partial migration rather than a stable clean
   separation.
4. Commander continues to look like the stack's accumulation point:
   - Telegram routing
   - cron delivery
   - mixed desktop sessions
   - broader cross-surface history
5. This makes Commander the most likely seat for hidden context carryover and
   lane-boundary confusion.

## Current Root-Cause Ranking

1. Ownership drift between documented roles and actual persisted routing/runtime surfaces
2. Commander becoming the operational accumulation sink for multiple lanes
3. Shared runtime drift plus dirty update/launch lifecycle in `hermes-agent`
4. Scheduler/launcher ownership failure for profile gateways
5. Stale profile runtime truth (`gateway.pid` / `gateway_state.json`)
6. Duplicate gateway/control-plane copies and Telegram polling conflicts
7. Recursive workspace duplication in `site-studio`
8. Browser `/chat` session reuse through `claude-last-session`
9. Inconsistent local API probing / bridge expectations
10. OpenRouter throttling amplified by orchestration behavior
11. GPU/driver issue as a secondary concern rather than the leading cause

## GPU / Driver Read

No fresh GPU-negative evidence appeared during this pass.

The evidence continues to cluster around routing, channel ownership, stale
control-plane state, and session accumulation rather than GPU execution.



