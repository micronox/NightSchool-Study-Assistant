# Check-In 2026-06-17 19:23:47

- Automation: `wsl-stack-check-in`
- Scope: latest pre-rebuild machine snapshot after the stack was reported offline

## Executive Delta

The runtime posture changed again since the last June 13 check-ins.

At this snapshot:

- `8645` is no longer listening
- `8646` is no longer listening
- only `11434` is still listening
- Docker containers are still down

However, Commander's state file was refreshed on June 17 and reports a healthy
surface:

- `api_server.state`: `connected`
- `telegram.state`: `connected`

So the system is now even more internally inconsistent than before:

- the old split Commander/Apollo local API listeners are gone
- the local state files were refreshed more recently
- Commander claims both API and Telegram are connected

## Current Runtime State

### Listening ports observed now

- `11434`
  - Ollama
  - PID `53644`

Not listening now:

- `8645`
- `8646`
- `3000`
- `3001`
- `8092`
- `9090`

### Containers

`docker ps` returned no running containers.

## State File Evidence

### Commander

Current file timestamps:

- `$USER_HOME\.hermes\profiles\commander\channel_directory.json`
  - `2026-06-17 6:55:47 AM`
- `$USER_HOME\.hermes\profiles\commander\gateway_state.json`
  - `2026-06-17 6:21:44 AM`

Current Commander state:

- `gateway_state`: `running`
- `active_agents`: `0`
- `api_server.state`: `connected`
- `telegram.state`: `connected`
- `updated_at`: `2026-06-17T11:21:44.690924+00:00`
- recorded PID:
  - `20168`

### Apollo

Current file timestamps:

- `$USER_HOME\.hermes\profiles\apollo\channel_directory.json`
  - `2026-06-17 7:00:26 AM`
- `$USER_HOME\.hermes\profiles\apollo\gateway_state.json`
  - `2026-06-17 6:21:23 AM`

## Interpretation

This is now a third distinct runtime phase in the incident timeline:

1. Early phase:
   - containerized inference/observability alive
   - control plane largely absent
2. Middle phase:
   - Commander/Apollo local API surfaces alive on `8645`/`8646`
   - Commander Telegram degraded and retrying
3. Current phase:
   - only Ollama visibly listening
   - containerized surfaces still absent
   - Commander state file refreshed and claims connected API + Telegram

That means the stack reached a point where:

- live ports
- running processes
- and internal state files

no longer align in a trustworthy way.

## Driver / GPU Evidence

Fresh WSL-side sample:

- VRAM used:
  - `13222 MiB`
- GPU utilization:
  - `22 %`

This is noticeably higher than many of the earlier low-GPU snapshots even though
the port map is now much smaller.

## New Conclusions Since Last Check-In

1. The split Commander/Apollo local API phase appears to have ended.
2. Commander's state file still reports a healthy connected posture despite the
   corresponding listener not appearing in the port scan.
3. Internal truth surfaces are now less trustworthy than ever.
4. This strengthens the decision to rebuild from scratch instead of trying to
   salvage live runtime state.

## Recommended Next Moves

1. Treat this check-in as the final pre-rebuild runtime snapshot.
2. Preserve Commander and Apollo state files as forensic evidence.
3. Do not reuse any live runtime state directly in the rebuild.


