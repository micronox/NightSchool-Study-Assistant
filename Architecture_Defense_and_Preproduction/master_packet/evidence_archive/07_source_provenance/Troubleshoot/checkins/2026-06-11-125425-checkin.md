# WSL Stack Check-In

- Timestamp: 2026-06-11T12:54:25-05:00
- Automation: `wsl-stack-check-in`
- Investigator: Northstar (debug/rebuild mode)

## Summary

This pass looked at the desktop/browser-facing side of the stack rather than
only the gateway runtime.

The biggest new finding is that Commander's saved desktop session ledger is a
heavily mixed backlog of sources and models:

- `cron`
- `cli`
- `telegram`
- `tui`
- `api_server`

That supports the broader diagnosis that the old system blurred boundaries
between surfaces that should have been easier to distinguish and reset.

## Runtime State

### Still alive

- `3000` Open WebUI
- `3001` Grafana
- `8092` `anvil-26b-server`
- `9090` Prometheus
- `11434` native Ollama

### Still absent

- `3006`, `3210`, `8089`, `8642`, `8643`, `8644`, `8645`, `8646`, `9119`

## Fresh Evidence

### Commander desktop sessions show strong cross-surface mixing

`C:\Users\larry\.hermes\profiles\commander\desktop\sessions.json` contains a
large mixed session backlog including:

- `cron_*` sessions
- `cli` sessions
- `telegram` sessions
- `tui` sessions
- `api_server` sessions

Examples of distinct models in the same saved session ledger:

- `deepseek/deepseek-v4-pro`
- `gemma-4-E2B-it-UD-Q4_K_XL.gguf`
- `nvidia/nemotron-3-super-120b-a12b`

Interpretation:

- Commander's desktop state acted like a shared session sink across multiple
  execution surfaces and model eras
- that makes it easier for stale or resumed context to feel “normal” even when
  it is crossing boundaries that should have been cleaner

### Apollo desktop ledger is much smaller and cleaner

`C:\Users\larry\.hermes\profiles\apollo\desktop\sessions.json` only showed a
small number of saved sessions by comparison.

That asymmetry matters:

- Commander looks like the primary accumulation point for mixed runtime history
- Apollo looks more constrained at the desktop session layer

This strengthens the idea that Commander is the more likely seat for context
bloat and cross-surface session confusion.

### Jarvis dashboard still assumes a richer live system than exists

The Jarvis dashboard codebase under `L:\Jarvis\dashboard` still clearly expects:

- primary local target on `http://127.0.0.1:3006`
- `/api/system/snapshot`
- monitored ports including:
  - `3006`
  - `3210`
  - `8089`
  - `8092`
  - `8642`
  - `8643`
  - `11434`

It also expects structured snapshot sections like:

- `lanes`
- `models`
- `memory`
- `services`
- `alerts`
- `captains`
- `requests`

Interpretation:

- Jarvis is designed around a fuller live mission-control world than the
  machine currently provides
- this increases the odds that Jarvis became another consumer of stale or
  partially valid assumptions rather than a source of fresh truth

### Jarvis code still explicitly centers `:3006`

The dashboard project still documents and tests against `:3006` as the primary
shell.

Because `3006` is still absent live, the dashboard remains another example of:

- healthy-looking code and docs
- absent runtime

### Session-bloat documentation remains aligned with what the desktop ledger suggests

The prior documented root cause:

- `/chat` restoring `claude-last-session` from localStorage

now fits even better in context because Commander also has a large saved desktop
ledger spanning multiple sources and models. Even if localStorage was the direct
trigger, the underlying saved-session environment was already primed for
confusion and accidental resumption.

## New Conclusions Since Last Check-In

1. Commander's desktop session store is a major evidence source for boundary
   collapse: it mixes multiple surfaces, models, and operating modes in one
   saved ledger.
2. Apollo looks more contained at the desktop-session layer, which further
   isolates Commander as the higher-risk context-bloat seat.
3. Jarvis still expects a richer and healthier live control plane than the
   machine actually exposes.
4. The session-bloat problem is likely not just one browser localStorage bug;
   that bug landed in a system that was already accumulating mixed-session
   history across many surfaces.

## Current Root-Cause Ranking

1. Ownership drift between documented roles and actual runtime surfaces
2. Shared runtime drift plus dirty update/launch lifecycle in `hermes-agent`
3. Scheduler/launcher ownership failure for profile gateways
4. Stale profile runtime truth (`gateway.pid` / `gateway_state.json`)
5. Commander-side mixed session accumulation across desktop/cron/cli/telegram/tui/api_server
6. Duplicate gateway/control-plane copies and Telegram polling conflicts
7. Recursive workspace duplication in `site-studio`
8. Browser `/chat` session reuse through `claude-last-session`
9. Inconsistent local API probing / bridge expectations
10. OpenRouter throttling amplified by orchestration behavior
11. GPU/driver issue as a secondary concern rather than the leading cause

## GPU / Driver Read

No fresh GPU-negative evidence appeared during this pass.

The live/stable surfaces remain the same local model and observability lanes,
while the instability keeps clustering around saved session state and the
missing control-plane surfaces.

