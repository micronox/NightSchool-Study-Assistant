# Check-In 2026-06-13 09:39:49

- Automation: `wsl-stack-check-in`
- Scope: continued high-signal persistence check on the sustained mixed mode

## Executive Delta

There is still no material break in the pattern.

The current-state runtime map remains:

- Commander on `8645`
- Apollo on `8646`
- Ollama on `11434`

The containerized surfaces remain absent:

- `3000`
- `3001`
- `8092`
- `9090`

The routing/state split remains:

- `channel_directory.json` keeps updating
- `gateway_state.json` remains frozen on June 12

This has now repeated across enough consecutive hourly observations to treat it
as a stable operating condition of the broken stack.

## Live Runtime Snapshot

### Listening ports

- `8645`
  - Commander
  - PID `30756`
- `8646`
  - Apollo
  - PID `30732`
- `11434`
  - Ollama
  - PID `66340`

### CPU accumulation

Current totals:

- Commander gateway PID `30756`
  - `343.64`
- Apollo gateway PID `30732`
  - `305.81`
- Ollama PID `66340`
  - `4.33`

All three continue to persist, with Commander and Apollo still accumulating CPU
time.

## Routing vs State Drift

### Fresh routing writes

- Commander `channel_directory.json`
  - `2026-06-13 9:34:58 AM`
- Apollo `channel_directory.json`
  - `2026-06-13 9:35:36 AM`

### Frozen state files

- Commander `gateway_state.json`
  - `2026-06-12 11:11:31 AM`
- Apollo `gateway_state.json`
  - `2026-06-12 11:11:10 AM`

### Interpretation

The routing layer remains the only clearly advancing runtime bookkeeping surface
in the legacy stack.

## Container / GPU State

`docker ps` still shows no running containers.

Fresh WSL-side sample:

- VRAM used:
  - `6796 MiB`
- GPU utilization:
  - `5 %`

Still low and stable.

## New Conclusions Since Last Check-In

1. The mixed-mode runtime remains unchanged for yet another hourly interval.
2. Channel-directory updates continue to be the most reliable indicator of
   current routing activity.
3. The live Commander/Apollo/Ollama trio should now be considered the
   authoritative current-state runtime map unless a real transition occurs.
4. Container absence remains durable and should be treated as part of the
   current operating mode.

## Recommended Next Moves

1. Preserve this repeated hourly pattern as evidence that the broken stack can
   sustain a long-lived but internally inconsistent runtime mode.
2. Continue using:
   - live ports
   - process CPU accumulation
   - channel-directory timestamps
   as the primary active-truth surfaces.
3. Keep `gateway_state.json` categorized as stale bookkeeping rather than live
   truth until a contradictory update appears.
