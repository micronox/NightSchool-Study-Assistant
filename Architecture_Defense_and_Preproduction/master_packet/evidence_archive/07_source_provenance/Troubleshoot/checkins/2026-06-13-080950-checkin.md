# Check-In 2026-06-13 08:09:50

- Automation: `wsl-stack-check-in`
- Scope: validate continued mixed-mode persistence and distinguish live routing
  activity from task/log activity

## Executive Delta

The system remains in the same mixed runtime mode:

- Commander live on `8645`
- Apollo live on `8646`
- Ollama live on `11434`
- containerized inference still absent

The useful nuance this round is that:

- channel-directory files continue to update on an hourly cadence
- gateway stderr logs are not being rewritten in the same cadence
- scheduled-task success still points back to the June 12 start event only

That suggests the current live behavior is not being driven by repeated task
restarts. It is more consistent with long-lived gateway processes continuing to
run while only some state surfaces refresh.

## Scheduler Evidence

Both gateway tasks still show:

- `LastRunTime`: `June 12, 2026 11:09:01 AM`
- `LastTaskResult`: `0`
- `NextRunTime`: blank

Interpretation:

- the tasks launched successfully on June 12
- there is no evidence of a subsequent scheduled re-run
- the currently alive gateway processes are still descendants of that June 12
  launch window

## Live Runtime Evidence

### Listening ports

- `8645`
  - Commander
  - PID `30756`
- `8646`
  - Apollo
  - PID `30732`
- `11434`
  - Ollama
  - PID `66340`

### CPU accumulation

Current totals:

- Commander gateway PID `30756`
  - `321.83`
- Apollo gateway PID `30732`
  - `286.39`
- Ollama PID `66340`
  - `4.05`

This continues the pattern of live Commander/Apollo gateway work over time.

## File Activity

### Fresh channel-directory writes

- Commander `channel_directory.json`
  - `2026-06-13 8:04:57 AM`
- Apollo `channel_directory.json`
  - `2026-06-13 8:05:35 AM`

### Stale stderr logs

- Commander `gateway-stderr.log`
  - last written `2026-06-09 2:32:24 PM`
- Apollo `gateway-stderr.log`
  - last written `2026-06-07 2:11:43 AM`

### Interpretation

This is a useful distinction:

- live routing metadata is still changing
- live processes are still consuming CPU
- but the historical stderr logs we examined earlier are not being actively
  appended during this current revived state

That means the strongest current signals of activity are:

- port/process presence
- CPU accumulation
- channel-directory timestamps

not:

- gateway state files
- old stderr log timestamps

## Driver / GPU Evidence

Fresh WSL-side sample:

- VRAM used:
  - `6260 MiB`
- GPU utilization:
  - `8 %`

Still low and stable.

## New Conclusions Since Last Check-In

1. The revived control plane still appears to be one sustained run from June 12,
   not a series of later task restarts.
2. Channel-directory updates remain the clearest current sign of ongoing routing
   activity.
3. Historical stderr logs are useful for prior failure modes, but they are not
   the best surface for proving current minute-to-minute activity in this state.
4. The machine continues to show low GPU pressure while the live control plane
   persists.

## Recommended Next Moves

1. Keep distinguishing between:
   - sustained live process activity
   - scheduled restart activity
   - historical error-log evidence
2. Treat channel-directory updates plus CPU accumulation as the strongest active
   evidence for the current mixed mode.
3. Preserve the June 12 launch window as the anchor event for the revived
   Commander/Apollo state.
