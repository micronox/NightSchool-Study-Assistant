# Check-In 2026-06-11 15:54:55

- Automation: `wsl-stack-check-in`
- Scope: local Hermes/Apollo/Anvil/Onyx stack heartbeat refresh focused on
  recursive workspace duplication, channel ownership, and stale runtime-state
  evidence

## Current Findings

The legacy runtime still shows the same overall shape: healthy inference and
observability surfaces, broken control-plane surfaces, and persistent evidence
of ownership collapse around Commander.

## New Evidence

### Recursive workspace duplication remains present at scale

A fresh measurement under:

- `L:\WSL\site-studio\projects`

still found:

- `427` recursively nested directories matching the known self-copy pattern

This confirms the contamination is not hypothetical or historical only. The
project tree still carries a large recursive-duplication footprint and should
continue to be treated as evidence, not a clean runtime source.

### Commander still owns the Mercury-linked Telegram group surface

Current contents of:

- `C:\Users\larry\.hermes\profiles\commander\channel_directory.json`

still map Telegram channels to Commander, including:

- `479877962`
  - `KryptoKoolAid`
- `-1003858086422`
  - `Apollo Mercury Ops`

This is important because it preserves one of the strongest operational clues in
the whole investigation:

- Mercury-linked Telegram routing did not remain isolated
- Commander became the live routing owner for the `Apollo Mercury Ops` group

That continues to support the theory that cross-surface chatter and burst
behavior could have been amplified by control-plane ownership collapse.

### GPU baseline remains steady

Fresh WSL-side sample:

- VRAM used:
  - `26221 MiB`
- GPU utilization:
  - `6 %`

This remains consistent with the earlier pattern:

- high baseline VRAM residency
- low actual compute utilization

That still points away from "insufficient hardware" and toward activation-path,
runtime, or host-side orchestration issues.

## Runtime State

No evidence this round suggests any recovery of the broken control-plane
surfaces.

Still considered live:

- `3000`
- `3001`
- `8092`
- `9090`
- `11434`

Still considered absent or not restored:

- `3006`
- `3210`
- `8012`
- `8013`
- `8089`
- `8642`
- `8643`
- `8644`
- `8645`
- `8646`
- `9102`
- `9119`
- `11435`

## Stale Runtime-State Note

I attempted a simplified re-probe of the legacy `gateway.pid` /
`gateway_state.json` situation for Commander and Apollo. The probe did not emit
clean output this round, so there is no new contradictory evidence yet.

Because earlier passes already showed:

- dead PIDs in `gateway.pid`
- `gateway_state.json` still claiming `running`

the working assumption does not change:

- legacy Hermes runtime state remains low-trust until disproven

## Suspected Root Causes

### Highest-confidence

- duplicate gateway/control-plane ownership
- stale runtime truth after failed launches
- session and routing contamination around Commander
- recursive workspace duplication in `site-studio`
- docs/runtime/port drift

### Still plausible amplifiers

- WSL2 memory remap behavior
- Docker/Windows/WSL boundary effects
- host-side service exposure through multiple networking layers

## New Conclusions Since Last Check-In

1. Recursive project contamination is still active in the evidence tree.
   - The nested-copy footprint remains large enough to justify quarantine.

2. Commander remains the most important operational gravity well.
   - It still owns the Mercury-linked Telegram group mapping.
   - This keeps supporting the theory that Mercury/Apollo/Commander state
     boundaries collapsed in practice.

3. The GPU story remains secondary.
   - Baseline VRAM use is high, but compute usage stays low.
   - The strongest evidence still points to orchestration and ownership failure
     before raw hardware shortage.

## Recommended Next Moves

1. Keep `site-studio` in the evidence-only category during rebuild.
2. Keep Mercury explicitly quarantined until a clean owner and boundary model is
   re-established.
3. Continue prioritizing the clean external `llama.cpp` lane as the first live
   rebuild surface outside the contaminated Onyx runtime.
